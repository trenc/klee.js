{
  "name": "klee.js",
  "version": "0.1.0-rc3",
  "main": "src/klee.js",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -fR dist && mkdir dist",
    "bundle:min": "esbuild src/klee.js --bundle --format=esm --sourcemap --minify --outfile=dist/klee.min.js",
    "bundle:js": "esbuild src/klee.js --bundle --format=esm --outfile=dist/klee.js",
    "bundle:watch": "esbuild src/klee.js --bundle --watch --format=esm --outfile=dist/klee.js",
    "bundle": "npm run bundle:js && npm run bundle:min",
    "serve": "browser-sync start -s -f 'example/*.html, dist/klee.js' --startPath 'example/'",
    "build": "npm run clean && npm run bundle",
    "start": "npm run clean && npm run bundle:watch & npm run serve",
    "test:serve": "browser-sync start --port 8000 -s --no-ui --no-open",
    "test:cy:open": "npx cypress open",
    "test:cy:run": "npx cypress run",
    "test:lint": "eslint src/*.js src/modules/*.js",
    "test": "npm run test:lint & npm run test:cy:run"
  },
  "author": "Tommy Schmucker",
  "license": "AGPL",
  "description": "A data driven generator and wrapper for three.js",
  "eslintConfig": {
    "env": {
      "browser": true,
      "es2021": true
    },
    "globals": {
      "cy": true
    },
    "extends": [
      "standard",
      "plugin:cypress/recommended"
    ],
    "parserOptions": {
      "ecmaVersion": 12,
      "sourceType": "module"
    },
    "rules": {
      "semi": [
        "error",
        "always"
      ],
      "indent": [
        "error",
        "tab",
        {
          "SwitchCase": 1
        }
      ],
      "no-tabs": [
        "error",
        {
          "allowIndentationTabs": true
        }
      ],
      "padded-blocks": [
        "error",
        "always"
      ]
    },
    "overrides": [
      {
        "files": "*.test.js",
        "rules": {
          "no-unused-expressions": "off"
        }
      }
    ]
  },
  "devDependencies": {
    "browser-sync": "^2.26.14",
    "cypress": "^7.3.0",
    "esbuild": "^0.11.11",
    "eslint": "^7.24.0",
    "eslint-config-standard": "^16.0.2",
    "eslint-plugin-cypress": "^2.11.3",
    "eslint-plugin-import": "^2.22.1",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-promise": "^4.3.1"
  }
}
